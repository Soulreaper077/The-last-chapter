<article class="wishlist">
    <div class="whole-list">
        <div class="info-section">
            <div class="list-name">
                <a href="/wishlist/{{id}}" class="links">{{title}}</a>
            </div>
            <div class="created-by">
                <p id="book-subtitle">
                   Created by <b>{{user.username}} </b> on <b>{{user.createdAt}}</b>
                </p>
            </div>
        </div>
    </div>
</article>